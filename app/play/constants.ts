/**
 * Which content pack `/play` currently runs. The Kindle Life chapters land
 * here one at a time later; today it's the non-canonical demo pack that
 * exercises every engine system end to end.
 */
export const ACTIVE_PACK_ID = "demo";
